{
  "name": "daily-digest-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:init": "wrangler d1 execute daily-digest --file=schema.sql",
    "db:init:remote": "wrangler d1 execute daily-digest --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.84.1"
  }
}
